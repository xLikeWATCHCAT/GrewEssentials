package net.dev.utils.sound;

import net.dev.*;
import org.bukkit.*;
import org.bukkit.entity.*;

public class CommandSoundsPreview {
    public static void stopSounds(Player clicker) {
        Bukkit.getScheduler().runTaskAsynchronously(GrewEssentials.getPlugin(), () -> {

        });
    }
}
